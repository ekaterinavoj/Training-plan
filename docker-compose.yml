services:
  training-plan:
    build: .
    container_name: training-plan
    restart: unless-stopped
    ports:
      - "3100:3100"
    environment:
      - PORT=3100
      - ADMIN_USERNAME=trainer936499
      - ADMIN_PASSWORD=SilaHubnuti-26x!
      - ADMIN_RESET_CODE=ObnovaHesla-9427-Trenink
    volumes:
      - ./data:/app/data
